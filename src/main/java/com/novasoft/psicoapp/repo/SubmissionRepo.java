package com.novasoft.psicoapp.repo; import com.novasoft.psicoapp.model.Submission; import org.springframework.data.jpa.repository.*; import java.util.*; public interface SubmissionRepo extends JpaRepository<Submission,Long>{ List<Submission> findByStudentId(Long id); List<Submission> findByStatus(String status); Optional<Submission> findByAssignmentIdAndStudentId(Long a, Long s); }
