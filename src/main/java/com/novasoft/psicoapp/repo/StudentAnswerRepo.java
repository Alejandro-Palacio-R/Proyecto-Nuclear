package com.novasoft.psicoapp.repo; import com.novasoft.psicoapp.model.StudentAnswer; import org.springframework.data.jpa.repository.*; import java.util.*; public interface StudentAnswerRepo extends JpaRepository<StudentAnswer,Long>{ List<StudentAnswer> findBySubmissionId(Long id); List<StudentAnswer> findBySubmissionIdAndQuestionId(Long submissionId,Long questionId); List<StudentAnswer> findByQuestionId(Long questionId); }
