package com.novasoft.psicoapp.repo; import com.novasoft.psicoapp.model.Assignment; import org.springframework.data.jpa.repository.*; import java.util.*; public interface AssignmentRepo extends JpaRepository<Assignment,Long>{ List<Assignment> findByStudentId(Long id); List<Assignment> findByGroupId(Long id); List<Assignment> findByStudentIdAndCaseStudyId(Long studentId,Long caseStudyId); List<Assignment> findByGameSessionId(Long gameSessionId); List<Assignment> findByStudentIdAndGameSessionId(Long studentId,Long gameSessionId); }
